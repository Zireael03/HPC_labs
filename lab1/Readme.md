### MatMul
Алгоритм распараллелен на уровне элементов результирующей матрицы. Каждый поток CUDA вычисляет одно значение C[i][j] как скалярное произведение i-й строки матрицы A и j-го столбца матрицы B. Поскольку матрица является двумерным объектом, то и сетка ядра тоже была выбюрана двумерной.